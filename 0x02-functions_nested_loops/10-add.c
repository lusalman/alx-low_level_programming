#include"main.h"
/**
 * add - adds two integers
 * @a: adds the first number
 * @b: adds the second number
 * Return : addition of both
 */
int add(int a, int b)
{
	int n;

	n = a + b;
	return (n);
}
